export default function styles(theme) {
    return {
        container: {
            position: "relative",
            height: "100%",
            overflow: "hidden"
        },
        image: {
            transform: "scale(2)",
            minHeight: "100%",
            width: "100%"
        }
    }
}