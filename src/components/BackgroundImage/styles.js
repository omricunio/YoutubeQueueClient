export default function styles(theme) {
    return {
        container: {
            position: "relative",
            height: "100%",
            overflow: "hidden"
        },
        image: {
            transform: "scale(2)"
        },
        children: {
            position: "absolute"
        }
    }
}