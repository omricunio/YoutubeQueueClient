export default theme => ({
    searchIcon: {
        height: "100%",
        marginRight: theme.spacing(1)
    },
    container: {
        display: "flex",
        alignItems: "center",
        padding: theme.spacing(0,0,1,0.5)
    },
    inputBase: {
        width: "90%"
    }
});