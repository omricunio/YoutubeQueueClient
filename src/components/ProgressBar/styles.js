export default theme => ({
    slider: {
        position: "absolute",
        top: "-11px",
        zIndex: 1,
        track: {
            color:"transparent"
        },
        rail: {
            color:"transparent"
        }
    }
});