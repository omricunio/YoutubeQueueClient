export default function styles(theme) {
    return {
        container: {
            position: "relative",
            height: "100%",
            overflow: "hidden",
            pointerEvents: "none"
        },
        video: {
            height: "100%"
        },
        hidden: {
            display: "none"
        }
    }
}