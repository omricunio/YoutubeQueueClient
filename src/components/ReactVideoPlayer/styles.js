export default function styles(theme) {
    return {
        container: {
            position: "relative",
            height: "100%",
            overflow: "hidden"
        },
        video: {
            height: "100%"
        }
    }
}