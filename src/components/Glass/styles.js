export default function styles(theme) {
    return {
        root: {
            position: "absolute",
            top: 0,
            height: "100%",
            width: "100%"
        },
        notClear: {
            backdropFilter: "blur(20px)"
        }
    };
}